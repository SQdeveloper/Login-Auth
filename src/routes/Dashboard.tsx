import DefaultLayaut from "../components/DefaultLayaut";

const Dashboard = () => {
    return (
        <DefaultLayaut>
            <h1>Dashboard</h1>
        </DefaultLayaut>
    );
};

export default Dashboard;